function reportsView(){const ex=state.expenses, inc=state.transactions.filter(x=>x.type==="income");const cats=expenseCategories.map(c=>({c,n:sum(ex.filter(x=>x.category===c),"amount")})).filter(x=>x.n).sort((a,b)=>b.n-a.n).slice(0,7);const max=Math.max(1,...cats.map(x=>x.n));return `<div class="dashboard-head"><div><span class="eyebrow">Insights</span><h2>Reports</h2><p class="subtext">Understand where your money is going.</p></div><div class="toolbar"><button class="btn" data-action="export-csv">Export CSV</button><button class="btn primary" data-action="print">Print</button></div></div><div class="grid grid-3">${metric("All income",money(sum(inc,"amount")),"↗","positive","Recorded income")}${metric("All expenses",money(sum(ex,"amount")),"↘","negative","Recorded expenses")}${metric("Net",money(sum(inc,"amount")-sum(ex,"amount")),"◈",sum(inc,"amount")>=sum(ex,"amount")?"positive":"negative","Income minus expenses")}</div><div class="grid grid-2 section-gap"><div class="card"><div class="card-header"><h3>Expense categories</h3></div><div class="chart-wrap">${cats.length?cats.map(x=>`<div class="bar-col"><b class="bar" style="height:${Math.max(5,x.n/max*100)}%"></b><span class="bar-label">${esc(x.c.slice(0,8))}</span></div>`).join(""):`<div class="empty">Add expenses to see the chart.</div>`}</div></div><div class="card"><div class="card-header"><h3>Top categories</h3></div><div class="list">${cats.map(x=>`<div class="list-row"><span class="row-title">${esc(x.c)}</span><b>${money(x.n)}</b></div>`).join("")||`<div class="empty">No data yet.</div>`}</div></div></div>`}
function exportCSV(){const rows=[["Date","Type","Category","Description","Amount"],...state.transactions.map(x=>[x.date,x.type,x.category||"",x.description||"",x.amount])];const csv=rows.map(r=>r.map(csvEscape).join(",")).join("\n");const blob=new Blob([csv],{type:"text/csv"}),a=document.createElement("a");a.href=URL.createObjectURL(blob);a.download="finance-transactions.csv";a.click();URL.revokeObjectURL(a.href)}