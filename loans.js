function loansView(){const borrowed=sum(state.loans,"amount"),paid=state.loans.filter(x=>x.status==="Paid").reduce((t,x)=>t+Number(x.amount),0),lent=sum(state.lent,"amount"),received=state.lent.filter(x=>x.status==="Received").reduce((t,x)=>t+Number(x.amount),0);return `<div class="dashboard-head"><div><span class="eyebrow">Borrowed & lent</span><h2>Loans</h2><p class="subtext">Keep every personal loan in one place.</p></div><div class="toolbar"><button class="btn" data-action="add-lent">＋ Money lent</button><button class="btn primary" data-action="add-loan">＋ Borrowed</button></div></div><div class="grid grid-4">${metric("Borrowed",money(borrowed),"↙","negative",`${state.loans.length} records`)}${metric("Repaid",money(paid),"✓","positive","Complete history")}${metric("Money lent",money(lent),"↗","info",`${state.lent.length} records`)}${metric("Received",money(received),"✓","positive","Complete history")}</div><div class="grid grid-2 section-gap"><div class="card"><div class="card-header"><h3>Money I borrowed</h3></div>${loanTable(state.loans,false)}</div><div class="card"><div class="card-header"><h3>Money I lent</h3></div>${loanTable(state.lent,true)}</div></div>`}
function loanTable(items,isLent){return `<div class="table-wrap"><table class="data-table"><thead><tr><th>Person</th><th>Amount</th><th>Due</th><th>Status</th><th></th></tr></thead><tbody>${items.length?items.map(x=>`<tr><td><b>${esc(x.person)}</b><div class="row-meta">${esc(x.note||"")}</div></td><td>${money(x.amount)}</td><td>${dateFmt(x.dueDate)}</td><td><span class="status ${x.status==="Paid"||x.status==="Received"?"success":"warning"}">${esc(x.status)}</span></td><td>${(x.status==="Paid"||x.status==="Received")?"":`<button class="btn small" data-${isLent?"receive":"pay"}="${x.id}">Mark ${isLent?"received":"paid"}</button>`}</td></tr>`).join(""):`<tr><td colspan="5"><div class="empty">No records yet.</div></td></tr>`}</tbody></table></div>`}
function loanModal(isLent){const contacts=state.contacts;AppUI.formModal(isLent?"Record money lent":"Record borrowed money",`<form><div class="form-grid"><div class="field"><label>Person</label><input name="person" list="contactNames" required><datalist id="contactNames">${contacts.map(c=>`<option value="${esc(c.name)}">`).join("")}</datalist></div><div class="field"><label>Phone (optional)</label><input name="phone"></div><div class="field"><label>Amount (¥)</label><input name="amount" type="number" min="0" required></div><div class="field"><label>${isLent?"Date":"Borrow date"}</label><input name="date" type="date" value="${today()}"></div><div class="field"><label>Due date</label><input name="due" type="date"></div><div class="field full"><label>Notes</label><textarea name="note"></textarea></div></div><div class="form-actions"><button class="btn primary">Save</button></div></form>`,f=>{const x={id:uid(),person:f.get("person"),phone:f.get("phone"),amount:+f.get("amount"),date:f.get("date"),dueDate:f.get("due"),note:f.get("note"),status:isLent?"Waiting":"Not Paid"};(isLent?state.lent:state.loans).push(x);state.transactions.push({id:uid(),date:x.date,type:isLent?"money lent":"loan borrowed",amount:x.amount,description:`${isLent?"Lent to":"Borrowed from"} ${x.person}`});saveState();AppUI.closeModal();renderCurrentView();AppUI.toast("Record saved")})}