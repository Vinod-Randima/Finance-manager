let calendarCursor=new Date();
function calendarView(){const y=calendarCursor.getFullYear(),m=calendarCursor.getMonth(),first=new Date(y,m,1),last=new Date(y,m+1,0),start=first.getDay(),cells=[];for(let i=0;i<start;i++)cells.push(`<div class="cal-day muted-day"></div>`);for(let d=1;d<=last.getDate();d++){const k=`${y}-${String(m+1).padStart(2,"0")}-${String(d).padStart(2,"0")}`,status=state.salary.workDays[k]||"";cells.push(`<button class="cal-day" data-day="${k}"><div class="day-num">${d}</div>${status?`<span class="day-status ${status==="worked"?"worked":status==="holiday"?"holiday":status==="salary-day"?"salary-day":"off"}">${status.replace("-"," ")}</span>`:""}</button>`)}while(cells.length<42)cells.push(`<div class="cal-day muted-day"></div>`);return `<div class="dashboard-head"><div><span class="eyebrow">Work & pay</span><h2>Calendar</h2><p class="subtext">Tap a day to mark its status. Worked days calculate salary automatically.</p></div><div class="toolbar"><button class="btn" data-cal-prev>←</button><span class="pill">${new Intl.DateTimeFormat("en-US",{month:"long",year:"numeric"}).format(first)}</span><button class="btn" data-cal-next>→</button></div></div><div class="card"><div class="calendar-scroll"><div class="calendar-grid"><div class="cal-week">Sun</div><div class="cal-week">Mon</div><div class="cal-week">Tue</div><div class="cal-week">Wed</div><div class="cal-week">Thu</div><div class="cal-week">Fri</div><div class="cal-week">Sat</div>${cells.join("")}</div></div></div>`}
function calendarStatus(day){const current=state.salary.workDays[day]||"";const opts=["worked","off","holiday","salary-day",""];const next=opts[(opts.indexOf(current)+1)%opts.length];if(next)state.salary.workDays[day]=next;else delete state.salary.workDays[day];saveState();renderCurrentView();}