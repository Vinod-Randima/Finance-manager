function savingsView(){const s=state.savings;return `<div class="dashboard-head"><div><span class="eyebrow">Build your reserve</span><h2>Savings</h2><p class="subtext">Track your current savings and target.</p></div><button class="btn primary" data-action="edit-savings">✎ Update</button></div><div class="grid grid-3"><div class="card"><span class="metric-label">Current savings</span><div class="metric-value">${money(s.current)}</div></div><div class="card"><span class="metric-label">Monthly savings</span><div class="metric-value">${money(s.monthly)}</div></div><div class="card"><span class="metric-label">Saving goal</span><div class="metric-value">${money(s.target)}</div></div></div><div class="card section-gap"><div class="card-header"><h3>Goal progress</h3><b>${Math.round(pct(s.current,s.target))}%</b></div><div class="progress" style="height:12px"><i style="width:${pct(s.current,s.target)}%"></i></div><p class="subtext">${money(Math.max(0,s.target-s.current))} remaining</p></div>`}
function savingsModal(){AppUI.formModal("Update savings",`<form><div class="form-grid"><div class="field"><label>Current savings</label><input name="current" type="number" value="${state.savings.current}"></div><div class="field"><label>Monthly savings</label><input name="monthly" type="number" value="${state.savings.monthly}"></div><div class="field full"><label>Saving goal</label><input name="target" type="number" value="${state.savings.target}"></div></div><div class="form-actions"><button class="btn primary">Save</button></div></form>`,f=>{state.savings={current:+f.get("current"),monthly:+f.get("monthly"),target:+f.get("target")};saveState();AppUI.closeModal();renderCurrentView();AppUI.toast("Savings updated")})}